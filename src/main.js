import { removeBackground } from '@imgly/background-removal';
import './styles.css';
import './playful-theme.css';
import './tool-directory.css';
import './id-preview.css';
import './footer-compact.css';
import './collage-order.css';
import './watermark-tile.css';
import './batch-transform.css';

const app = document.querySelector('#app');
const MAX_BYTES = 10 * 1024 * 1024;
const MAX_PIXELS = 24_000_000;
const ONLINE_BG_API = 'https://photo-api.tinylabpro.com/v1/remove-background';
const specs = {
  inch1: { zh: '常用一寸 · 295 × 413', en: '1-inch · 295 × 413', w: 295, h: 413 },
  inch2: { zh: '常用二寸 · 413 × 579', en: '2-inch · 413 × 579', w: 413, h: 579 },
  passport: { zh: '护照常用 · 390 × 567', en: 'Passport common · 390 × 567', w: 390, h: 567 },
  square: { zh: '方形证件照 · 600 × 600', en: 'Square ID · 600 × 600', w: 600, h: 600 },
};

let lang = localStorage.getItem('photo_lang') || (navigator.language.startsWith('zh') ? 'zh' : 'en');
let mode = 'home';
let state = freshState();
function freshState(){ return { file:null, source:null, processed:null, outputW:295, outputH:413,
  zoom:1, offsetX:0, offsetY:0, rotate:0, flipX:1, flipY:1, background:'#ffffff', format:'image/png', quality:.9, batchFiles:[],
  removing:false, progress:0, status:'', objectUrls:[], watermarkText:'', watermarkColor:'#ffffff', bgMethod:'online',
  watermarkOpacity:.85, watermarkSize:42, watermarkX:42, watermarkY:88, watermarkTile:false, watermarkLogo:null,
  logoOpacity:.9, logoSize:22, logoX:82, logoY:82, collageImages:[], collageLayout:'grid', collageGap:18, sheetPreview:false }; }
const tr = (zh,en) => lang === 'zh' ? zh : en;

function toolIcon(type){
  const icons={
    id:'<rect x="4" y="3" width="16" height="18" rx="3"/><circle cx="12" cy="9" r="2.4"/><path d="M7.5 17c.8-2.5 2.3-3.8 4.5-3.8s3.7 1.3 4.5 3.8"/>',
    compress:'<path d="M5 7h14M5 12h14M5 17h14"/><path d="m9 4-3 3 3 3M15 14l3 3-3 3"/>',
    resize:'<rect x="5" y="5" width="14" height="14" rx="2"/><path d="M9 3H5a2 2 0 0 0-2 2v4m12 12h4a2 2 0 0 0 2-2v-4M3 3l5 5m13 13-5-5"/>',
    crop:'<path d="M7 3v14a4 4 0 0 0 4 4h10M3 7h14a4 4 0 0 1 4 4v10"/>',
    rotate:'<path d="M19 8V4l-2.3 2.3A8 8 0 1 0 20 12"/><path d="M19 4h-4"/>',
    format:'<path d="M7 3h8l4 4v14H7z"/><path d="M15 3v5h5M10 13h6M10 17h4"/>',
    watermark:'<rect x="4" y="5" width="16" height="14" rx="2.5"/><path d="M8 15l3-5 3 5m-4.8-1.8h3.6M16 9v6"/>',
    collage:'<rect x="4" y="4" width="6" height="6" rx="1"/><rect x="14" y="4" width="6" height="6" rx="1"/><rect x="4" y="14" width="6" height="6" rx="1"/><rect x="14" y="14" width="6" height="6" rx="1"/>',
    background:'<path d="M4 18 9.5 12l3.3 3.4 2.4-2.4L20 18"/><circle cx="8" cy="8" r="2"/><path d="M3 3h18v18H3zM16.5 5.5l2 2m-2 0 2-2"/>'
  };
  return `<svg viewBox="0 0 24 24" aria-hidden="true">${icons[type]||icons.format}</svg>`;
}

function shell(content){
  return `<div class="shell"><header class="hero"><nav class="nav"><div class="brand">Photo · ${tr('图片工坊','Photo Lab')}</div><div class="nav-actions"><button class="ghost" id="feedback">${tr('反馈','Feedback')}</button><button class="ghost coffee" id="coffee">☕ ${tr('请喝咖啡','Buy coffee')}</button><button class="ghost" id="lang">${lang==='zh'?'EN':'中文'}</button></div></nav><div class="hero-inner"><div class="eyebrow">TINY PRODUCT LAB · 小产品实验室</div><h1>${tr('把照片处理成刚刚好的样子','Make every photo fit its purpose')}</h1><p class="hero-copy">${tr('制作证件照、透明图片和 Logo。照片默认只在你的浏览器里处理，不上传服务器。','Create ID photos, transparent images and logos. Your photo stays in your browser by default and is not uploaded.')}</p><div class="privacy-line"><span>${tr('浏览器本地处理','Processed locally')}</span><span>${tr('无需注册','No account')}</span><span>${tr('清除图片元数据','Metadata removed')}</span></div></div></header><main class="main">${content}</main><footer class="footer"><section class="contact-card"><div><span class="contact-kicker">${tr('微信公众号 · 小产品实验室','WeChat · 小产品实验室')}</span><h3>${tr('扫码关注，获取工具更新','Scan for updates and tips')}</h3><p>${tr('遇到问题或有功能建议，欢迎公众号私信或邮件联系。','Questions and feature requests are welcome by WeChat or email.')}</p><a class="contact-email" href="mailto:userfeedback@zohomail.com">userfeedback@zohomail.com</a></div><img src="/wechat-qr.png" alt="小产品实验室微信公众号二维码"></section><div class="domain-memory"><p>${tr('域名助记，换台电脑也能找到','Domain breakdown')}</p><strong>photo.tinylabpro.com</strong><div class="domain-row"><span>photo</span><span>tiny</span><span>lab</span><span>pro</span></div><div class="domain-row meanings"><span>${tr('图片','photo')}</span><span>${tr('小','tiny')}</span><span>${tr('实验室','lab')}</span><span>${tr('专业','pro')}</span></div></div></footer><dialog id="coffeeDialog"><button class="dialog-close" id="closeCoffee">×</button><p class="contact-kicker">TINY PRODUCT LAB</p><h2>${tr('请开发者喝杯咖啡','Buy the developer a coffee')}</h2><p>${tr('感谢你的支持。收款功能暂时保留，等工具站更稳定后再开放。','Thank you for your support. Donations will open after the tools are more stable.')}</p></dialog></div>`;
}

function home(){
  const tools=[
    ['id','id',tr('证件照制作','ID photo maker'),tr('自动去背景、换底色、选尺寸，还能导出六寸排版照。','Remove backgrounds, choose a color and size, then export a print sheet.'),[tr('人像抠图','Portrait cutout'),tr('红白蓝底','Background colors')]],
    ['batch','compress',tr('图片压缩','Image compression'),tr('单张或批量压缩，清楚显示处理前后体积和节省比例。','Compress one or many images, with before-and-after size savings.'),[tr('批量处理','Batch'),tr('本地完成','Local')]],
    ['image','resize',tr('修改尺寸','Resize image'),tr('按像素调整宽高，也能在保留画面比例后重新导出。','Set exact pixel dimensions and export again with the right framing.'),[tr('像素尺寸','Pixels'),tr('比例缩放','Scale')]],
    ['image','crop',tr('图片裁剪','Crop image'),tr('自由裁剪或使用 1:1、4:3、16:9 等常用比例。','Crop freely or use common ratios such as 1:1, 4:3 and 16:9.'),['1:1','4:3','16:9']],
    ['image','rotate',tr('旋转与翻转','Rotate & flip'),tr('左旋、右旋、水平翻转、垂直翻转，一次处理完成。','Rotate left or right, flip horizontally or vertically in one place.'),[tr('左/右旋转','Rotate'),tr('水平/垂直翻转','Flip')]],
    ['batch','format',tr('格式转换','Format conversion'),tr('在 JPG、PNG、WebP 之间转换，支持一次处理多张图片。','Convert JPG, PNG and WebP, including batches of images.'),['JPG','PNG','WebP']],
    ['image','watermark',tr('图片加水印','Add watermark'),tr('添加文字或 Logo 水印，自由调整透明度、大小和位置。','Add text or logo watermarks with adjustable opacity, size and position.'),[tr('文字水印','Text'),tr('Logo 水印','Logo')]],
    ['collage','collage',tr('图片拼图','Collage maker'),tr('横向、纵向、网格或九宫格拼图；一张图片也可以重复排版。','Make horizontal, vertical, grid or nine-grid collages—even from one image.'),[tr('九宫格','Nine-grid'),tr('1–9 张','1–9 photos')]],
    ['image','background',tr('智能去背景','AI background removal'),tr('浏览器本地识别主体，导出透明 PNG 或替换为纯色底图。','Identify a subject locally in your browser and export transparent or colored images.'),[tr('本地模型','Local model'),tr('透明 PNG','Transparent PNG')]],
  ];
  app.innerHTML = shell(`<section class="tools-intro"><p class="contact-kicker">${tr('9 个浏览器本地图片工具','9 browser-based image tools')}</p><h2>${tr('选一个工具，上传图片就开始','Choose a tool, then upload a photo')}</h2><p>${tr('基础处理无需注册，图片默认不离开你的设备。','No sign-up for essentials; your image stays on your device by default.')}</p></section><div class="tool-grid">${tools.map(([tool,entry,title,description,tags])=>`<button class="card tool-card" data-tool="${tool}" data-entry="${entry}"><span class="arrow">→</span><span class="tool-icon">${toolIcon(entry)}</span><h2>${title}</h2><p>${description}</p><div class="tag-row">${tags.map(tag=>`<span class="tag">${tag}</span>`).join('')}</div></button>`).join('')}</div><section class="card info"><div class="info-grid"><div><h3>${tr('照片不会上传','Photos stay local')}</h3><p>${tr('编辑、抠图和导出都在浏览器里完成。首次智能抠图需要下载模型，之后会由浏览器缓存。','Editing, cutout and export happen in your browser. The model downloads once and is then cached.')}</p></div><div><h3>${tr('明确的输入限制','Clear limits')}</h3><p>${tr('支持 PNG、JPG、WebP；单张不超过 10 MB、2400 万像素。不接受 SVG 和 GIF。','PNG, JPG and WebP; up to 10 MB and 24 megapixels. SVG and GIF are not accepted.')}</p></div><div><h3>${tr('结果由你确认','You stay in control')}</h3><p>${tr('所有规格和质量检查都是辅助提示。正式证件要求请以办理机构的最新说明为准。','Presets and checks are guidance. Always verify the latest requirements of the issuing authority.')}</p></div></div></section>`);
  bindCommon();
  document.querySelectorAll('[data-tool]').forEach(el => el.onclick = () => openTool(el.dataset.tool,el.dataset.entry));
}

function openTool(next,entry=''){ mode=next; state=freshState(); state.entry=entry; if(next==='collage')return collageEditor(); if(next==='batch')return batchEditor(); if(next==='image'){state.outputW=1000;state.outputH=1000;state.background='transparent';} editor(); }

function entryHint(){
  const hints={
    resize:tr('上传后会打开尺寸和比例设置。','After upload, size and scale settings will be ready.'),
    crop:tr('上传后会打开裁剪比例和画面位置设置。','After upload, crop ratio and framing settings will be ready.'),
    rotate:tr('上传后会打开旋转和翻转设置。','After upload, rotate and flip controls will be ready.'),
    watermark:tr('上传后会打开文字和 Logo 水印设置。','After upload, text and logo watermark controls will be ready.'),
    background:tr('上传后可开始本地智能去背景。','After upload, you can start local background removal.'),
  };
  return hints[state.entry]||'';
}

function focusEntryControl(){
  const targets={resize:'#outW',crop:'#cropRatio',rotate:'#rotateLeft',watermark:'#watermarkText',background:'#removeBg'};
  const target=document.querySelector(targets[state.entry]);
  if(!target)return;
  target.closest('.control-section')?.scrollIntoView({behavior:'smooth',block:'start'});
  target.focus({preventScroll:true});
}

function editor(){
  const isId=mode==='id';
  app.innerHTML = shell(`<section class="card workspace"><div class="workspace-head"><button class="back" id="back">←</button><div class="workspace-title"><h2>${isId?tr('证件照制作','ID photo maker'):tr('图片与 Logo 处理','Image & logo tools')}</h2><p>${tr('图片只在当前浏览器中处理','The image is processed only in this browser')}</p></div></div><div class="editor"><aside class="controls"><div class="control-section"><h3>1 · ${tr('选择图片','Choose image')}</h3><label class="upload"><strong>${tr('点击选择照片','Click to choose a photo')}</strong><small>PNG / JPG / WebP · ≤ 10 MB</small><input id="file" type="file" accept="image/png,image/jpeg,image/webp"></label>${entryHint()?`<p class="entry-hint">${entryHint()}</p>`:''}</div><div id="editing" class="hidden">${isId?idControls():imageControls()}<div class="control-section"><h3>${isId?'4':'3'} · ${tr('导出','Export')}</h3><label class="field"><span class="field-label"><span>${tr('格式','Format')}</span></span><select id="format"><option value="image/png">PNG</option><option value="image/jpeg">JPG</option><option value="image/webp">WebP</option></select></label><label class="field" id="qualityField"><span class="field-label"><span>${tr('质量','Quality')}</span><output id="qualityOut">90%</output></span><input id="quality" type="range" min="50" max="100" value="90"></label><div class="row2"><button class="btn primary" id="download">${tr('下载电子图片','Download image')}</button>${isId?`<button class="btn secondary" id="sheet">${tr('六寸排版照','6-inch sheet')}</button>`:''}</div></div></div></aside><div class="canvas-area"><div class="canvas-toolbar"><span class="status" id="status">${tr('请选择一张照片开始','Choose a photo to start')}</span><span class="status" id="dimensions"></span></div><div class="canvas-wrap"><div class="empty" id="empty"><div class="empty-icon">▧</div><p>${tr('上传照片后，这里会显示实时预览。所有操作都可以反复调整。','Your live preview appears here after upload. Every setting remains adjustable.')}</p></div><canvas id="canvas" class="hidden"></canvas></div></div></div></section>`);
  bindCommon(); document.querySelector('#back').onclick=home; document.querySelector('#file').onchange=e=>loadFile(e.target.files[0]); if(!isId){const launch=document.createElement('button');launch.type='button';launch.className='btn secondary wide collage-launch';launch.textContent=tr('直接制作拼图','Make a collage instead');launch.onclick=()=>openTool('collage');document.querySelector('#file').closest('.upload').after(launch);} bindEditor();
}

function collageEditor(){
  app.innerHTML=shell(`<section class="card workspace"><div class="workspace-head"><button class="back" id="back">←</button><div class="workspace-title"><h2>${tr('图片拼图','Collage maker')}</h2><p>${tr('可选择 1–9 张图片；单张也能制作九宫格。','Choose 1–9 images; even one image can make a nine-grid.')}</p></div></div><div class="editor"><aside class="controls"><div class="control-section"><h3>1 · ${tr('选择图片','Choose photos')}</h3><label class="upload"><strong>${tr('选择图片','Choose photos')}</strong><small>PNG / JPG / WebP · ${tr('1–9 张均可','1–9 files')}</small><input id="collageFiles" type="file" multiple accept="image/png,image/jpeg,image/webp"></label></div><div id="collageControls" class="hidden"><div class="control-section"><h3>2 · ${tr('拼图样式','Layout')}</h3><label class="field"><span class="field-label">${tr('排版','Layout')}</span><select id="collageLayout"><option value="grid">${tr('网格拼图','Grid')}</option><option value="nine">${tr('九宫格（循环填充）','Nine-grid (repeat)')}</option><option value="horizontal">${tr('横向拼接','Horizontal')}</option><option value="vertical">${tr('纵向拼接','Vertical')}</option></select></label><label class="field"><span class="field-label"><span>${tr('间距','Gap')}</span><output id="gapOut">18 px</output></span><input id="collageGap" type="range" min="0" max="60" value="18"></label><label class="field"><span class="field-label">${tr('背景颜色','Background')}</span><input id="collageBg" type="color" value="#ffffff"></label></div><div class="control-section"><h3>3 · ${tr('导出','Export')}</h3><label class="field"><span class="field-label">${tr('格式','Format')}</span><select id="collageFormat"><option value="image/jpeg">JPG</option><option value="image/png">PNG</option><option value="image/webp">WebP</option></select></label><button class="btn primary wide" id="downloadCollage">${tr('下载拼图','Download collage')}</button></div></div></aside><div class="canvas-area"><div class="canvas-toolbar"><span class="status" id="status">${tr('选择图片开始拼图','Choose images to start')}</span><span class="status" id="dimensions"></span></div><div class="canvas-wrap"><div class="empty" id="empty"><div class="empty-icon">▦</div><p>${tr('上传 1–9 张图片；单图可用九宫格重复排版。','Upload 1–9 images; a single photo can repeat in a nine-grid.')}</p></div><canvas id="canvas" class="hidden"></canvas></div></div></div></section>`);
  bindCommon();document.querySelector('#back').onclick=()=>openTool('image');document.querySelector('#collageFiles').onchange=e=>loadCollageFiles([...e.target.files]);
}

function batchEditor(){
  app.innerHTML=shell(`<section class="card workspace"><div class="workspace-head"><button class="back" id="back">←</button><div class="workspace-title"><h2>${tr('批量压缩与转换','Batch compress & convert')}</h2><p>${tr('最多 20 张、总计不超过 100 MB；所有处理在浏览器中完成。','Up to 20 images and 100 MB total; processing stays in your browser.')}</p></div></div><div class="editor"><aside class="controls"><div class="control-section"><h3>1 · ${tr('选择图片','Choose images')}</h3><label class="upload"><strong>${tr('选择多张图片','Choose multiple images')}</strong><small>PNG / JPG / WebP · ${tr('最多 20 张','up to 20 files')}</small><input id="batchFiles" type="file" multiple accept="image/png,image/jpeg,image/webp"></label></div><div id="batchControls" class="hidden"><div class="control-section"><h3>2 · ${tr('输出设置','Output settings')}</h3><label class="field"><span class="field-label">${tr('输出格式','Format')}</span><select id="batchFormat"><option value="image/jpeg">JPG</option><option value="image/png">PNG</option><option value="image/webp">WebP</option></select></label><label class="field"><span class="field-label"><span>${tr('压缩质量','Quality')}</span><output id="batchQualityOut">82%</output></span><input id="batchQuality" type="range" min="30" max="100" value="82"></label><label class="field"><span class="field-label">${tr('最长边 px（留空则不缩放）','Longest edge px (blank keeps size)')}</span><input id="batchMaxEdge" type="number" min="64" max="4096" placeholder="${tr('例如 1600','e.g. 1600')}"></label></div><div class="control-section"><h3>3 · ${tr('开始处理','Process')}</h3><button class="btn primary wide" id="runBatch">${tr('处理并下载','Process and download')}</button><p class="notice" id="batchReport"></p></div></div></aside><div class="canvas-area"><div class="canvas-toolbar"><span class="status" id="status">${tr('选择图片开始','Choose images to start')}</span></div><div class="canvas-wrap"><div class="empty" id="empty"><div class="empty-icon">▦</div><p>${tr('适合批量压缩、统一尺寸和格式转换。处理后的文件会逐个下载。','Compress, resize and convert in one batch. Processed files download individually.')}</p></div></div></div></div></section>`);
  bindCommon();document.querySelector('#back').onclick=()=>openTool('image');document.querySelector('#batchFiles').onchange=e=>loadBatchFiles([...e.target.files]);
}

function loadBatchFiles(files){
  const total=files.reduce((sum,file)=>sum+file.size,0);if(!files.length||files.length>20||total>100*1024*1024)return alert(tr('请选择 1–20 张图片，且总大小不超过 100 MB。','Choose 1–20 images totaling no more than 100 MB.'));
  if(files.some(file=>!['image/png','image/jpeg','image/webp'].includes(file.type)||file.size>MAX_BYTES))return alert(tr('仅支持单张不超过 10 MB 的 PNG、JPG、WebP。','Each file must be PNG, JPG or WebP and under 10 MB.'));
  state.batchFiles=files;document.querySelector('#batchControls').classList.remove('hidden');document.querySelector('#empty').classList.add('hidden');installBatchTransformControls();setStatus(tr(`已选择 ${files.length} 张图片。`,`Selected ${files.length} images.`));
  document.querySelector('#batchQuality').oninput=e=>document.querySelector('#batchQualityOut').textContent=e.target.value+'%';document.querySelector('#runBatch').onclick=runBatch;
}

function installBatchTransformControls(){
  if(document.querySelector('#batchRotate'))return;const maxEdge=document.querySelector('#batchMaxEdge');if(!maxEdge)return;
  const fields=document.createElement('div');fields.className='batch-transform';fields.innerHTML=`<label class="field"><span class="field-label">${tr('旋转','Rotate')}</span><select id="batchRotate"><option value="0">${tr('不旋转','No rotation')}</option><option value="90">${tr('右旋 90°','90° clockwise')}</option><option value="180">180°</option><option value="270">${tr('左旋 90°','90° counterclockwise')}</option></select></label><div class="batch-flips"><label><input id="batchFlipX" type="checkbox">${tr('水平翻转','Flip horizontal')}</label><label><input id="batchFlipY" type="checkbox">${tr('垂直翻转','Flip vertical')}</label></div>`;maxEdge.closest('.field')?.after(fields);
}

async function runBatch(){
  const files=state.batchFiles,format=document.querySelector('#batchFormat').value,quality=+document.querySelector('#batchQuality').value/100,maxEdge=+document.querySelector('#batchMaxEdge').value||0,rotate=+document.querySelector('#batchRotate')?.value||0,flipX=document.querySelector('#batchFlipX')?.checked?-1:1,flipY=document.querySelector('#batchFlipY')?.checked?-1:1,report=document.querySelector('#batchReport'),button=document.querySelector('#runBatch');button.disabled=true;let before=0,after=0;
  try{for(let i=0;i<files.length;i++){const file=files[i],img=await blobToImage(file),scale=maxEdge&&Math.max(img.naturalWidth,img.naturalHeight)>maxEdge?maxEdge/Math.max(img.naturalWidth,img.naturalHeight):1,w=Math.round(img.naturalWidth*scale),h=Math.round(img.naturalHeight*scale),turns=rotate%180!==0,c=document.createElement('canvas');c.width=turns?h:w;c.height=turns?w:h;const ctx=c.getContext('2d');ctx.translate(c.width/2,c.height/2);ctx.rotate(rotate*Math.PI/180);ctx.scale(flipX,flipY);ctx.drawImage(img,-w/2,-h/2,w,h);const blob=await canvasBlob(c,format,quality),ext=format==='image/png'?'png':format==='image/webp'?'webp':'jpg';before+=file.size;after+=blob.size;saveBlob(blob,`photo-${i+1}.${ext}`);setStatus(tr(`正在处理 ${i+1} / ${files.length} 张…`,`Processing ${i+1} / ${files.length}…`));}const saved=Math.max(0,Math.round((1-after/before)*100));report.textContent=tr(`完成：${files.length} 张图片，${formatBytes(before)} → ${formatBytes(after)}，节省 ${saved}%。`,`Done: ${files.length} images, ${formatBytes(before)} → ${formatBytes(after)}, saved ${saved}%.`);setStatus(tr('批量处理完成。','Batch processing complete.'));}catch(e){report.textContent=tr('处理未完成，请减少图片数量或大小后重试。','Processing did not finish. Try fewer or smaller images.');}finally{button.disabled=false;}
}

async function loadCollageFiles(files){
  if(files.length<1)return alert(tr('请选择至少 1 张图片。','Choose at least 1 image.'));if(files.length>9)return alert(tr('一次最多选择 9 张图片。','You can choose up to 9 images.'));
  try{state.collageImages=[];for(const file of files){if(!['image/png','image/jpeg','image/webp'].includes(file.type)||file.size>MAX_BYTES)throw new Error(tr('图片格式不支持或超过 10 MB。','An image is unsupported or exceeds 10 MB.'));state.collageImages.push(await blobToImage(file));}document.querySelector('#collageControls').classList.remove('hidden');document.querySelector('#empty').classList.add('hidden');document.querySelector('#canvas').classList.remove('hidden');renderCollageOrderControls();document.querySelector('#collageLayout').onchange=e=>{state.collageLayout=e.target.value;drawCollage();};document.querySelector('#collageGap').oninput=e=>{state.collageGap=+e.target.value;document.querySelector('#gapOut').textContent=e.target.value+' px';drawCollage();};document.querySelector('#collageBg').oninput=e=>{state.background=e.target.value;drawCollage();};document.querySelector('#collageFormat').onchange=e=>state.format=e.target.value;document.querySelector('#downloadCollage').onclick=async()=>{const c=document.querySelector('#canvas'),ext=state.format==='image/png'?'png':state.format==='image/webp'?'webp':'jpg',blob=await canvasBlob(c);saveBlob(blob,`collage-${state.collageImages.length}.${ext}`);setStatus(tr(`已导出拼图 · ${formatBytes(blob.size)}`,`Collage exported · ${formatBytes(blob.size)}`));};setStatus(tr(`已选择 ${files.length} 张图片。`,`Selected ${files.length} images.`));drawCollage();}catch(e){alert(e.message||e);}
}
function renderCollageOrderControls(){
  const controls=document.querySelector('#collageControls');if(!controls)return;document.querySelector('#collageOrder')?.remove();
  const exportTitle=controls.querySelector('#downloadCollage')?.closest('.control-section')?.querySelector('h3');if(exportTitle)exportTitle.textContent=`4 · ${tr('导出','Export')}`;
  const section=document.createElement('div');section.className='control-section collage-order-section';section.id='collageOrder';
  section.innerHTML=`<h3>3 · ${tr('图片顺序','Image order')}</h3><p class="notice">${tr('按最终拼接顺序调整；九宫格会从第一张开始循环。','Set the final order; the nine-grid repeats from the first image.')}</p><div class="collage-order-list">${state.collageImages.map((_,index)=>`<div class="collage-order-item"><span>${index+1}</span><div class="order-actions"><button class="btn" type="button" data-move="up" data-index="${index}" ${index===0?'disabled':''}>↑</button><button class="btn" type="button" data-move="down" data-index="${index}" ${index===state.collageImages.length-1?'disabled':''}>↓</button></div></div>`).join('')}</div>`;
  controls.querySelector('.control-section')?.after(section);section.querySelectorAll('[data-move]').forEach(button=>button.onclick=()=>{const index=+button.dataset.index,target=button.dataset.move==='up'?index-1:index+1;[state.collageImages[index],state.collageImages[target]]=[state.collageImages[target],state.collageImages[index]];renderCollageOrderControls();drawCollage();});
}

function drawCollage(){const images=state.collageImages;if(!images.length)return;const outputImages=state.collageLayout==='nine'?Array.from({length:9},(_,i)=>images[i%images.length]):images,c=document.querySelector('#canvas'),ctx=c.getContext('2d'),n=outputImages.length,g=state.collageGap;let cols,rows;if(state.collageLayout==='horizontal'){cols=n;rows=1;}else if(state.collageLayout==='vertical'){cols=1;rows=n;}else if(state.collageLayout==='nine'){cols=3;rows=3;}else{cols=Math.ceil(Math.sqrt(n));rows=Math.ceil(n/cols);}const cell=600;c.width=cols*cell+(cols+1)*g;c.height=rows*cell+(rows+1)*g;ctx.fillStyle=state.background||'#fff';ctx.fillRect(0,0,c.width,c.height);outputImages.forEach((img,i)=>{const col=i%cols,row=Math.floor(i/cols),x=g+col*(cell+g),y=g+row*(cell+g),iw=img.naturalWidth||img.width,ih=img.naturalHeight||img.height,scale=Math.max(cell/iw,cell/ih),w=iw*scale,h=ih*scale;ctx.save();ctx.beginPath();ctx.rect(x,y,cell,cell);ctx.clip();ctx.drawImage(img,x+(cell-w)/2,y+(cell-h)/2,w,h);ctx.restore();});document.querySelector('#dimensions').textContent=`${c.width} × ${c.height} px`;}

function idControls(){
  return `<div class="control-section"><h3>2 · ${tr('去背景与底色','Remove & replace background')}</h3><button class="btn primary wide" id="removeBg">${tr('智能去除背景','Remove background')}</button><div class="progress hidden" id="progress"><i></i></div><p class="notice">${tr('首次使用需下载约 40 MB 模型。处理在本机完成，照片不会上传。','The first run downloads a ~40 MB model. Processing stays on your device.')}</p><div class="swatches" style="margin-top:12px"><button class="swatch active" data-bg="#ffffff" style="background:#fff" title="White"></button><button class="swatch" data-bg="#438edb" style="background:#438edb" title="Blue"></button><button class="swatch" data-bg="#d9303e" style="background:#d9303e" title="Red"></button><input id="bgCustom" type="color" value="#ffffff" title="Custom"></div></div><div class="control-section"><h3>3 · ${tr('规格与位置','Size & position')}</h3><label class="field"><span class="field-label"><span>${tr('常用规格','Preset')}</span></span><select id="spec">${Object.entries(specs).map(([k,v])=>`<option value="${k}">${v[lang]}</option>`).join('')}<option value="custom">${tr('自定义尺寸','Custom size')}</option></select></label><div class="row2"><label class="field"><span class="field-label">${tr('宽度 px','Width px')}</span><input id="outW" type="number" min="100" max="4096" value="295"></label><label class="field"><span class="field-label">${tr('高度 px','Height px')}</span><input id="outH" type="number" min="100" max="4096" value="413"></label></div>${positionControls()}<div class="quality-list" id="checks"></div><p class="notice">${tr('预设仅供常见场景参考，办理证件或报名时请核对机构最新要求。','Presets are general references. Verify the latest official requirements before submission.')}</p></div>`;
}
function imageControls(){
  return `<div class="control-section"><h3>2 · ${tr('裁剪与调整','Crop & adjust')}</h3><label class="field"><span class="field-label">${tr('裁剪比例','Crop ratio')}</span><select id="cropRatio"><option value="free">${tr('自由比例','Free')}</option><option value="1">1 : 1</option><option value="1.333">4 : 3</option><option value="1.778">16 : 9</option><option value="0.75">3 : 4</option><option value="0.563">9 : 16</option></select></label><p class="notice">${tr('选择比例后，可用下方缩放和位置拖动来决定保留区域。','Choose a ratio, then use scale and position below to choose the visible area.')}</p><div class="row2"><label class="field"><span class="field-label">${tr('宽度 px','Width px')}</span><input id="outW" type="number" min="32" max="4096" value="1000"></label><label class="field"><span class="field-label">${tr('高度 px','Height px')}</span><input id="outH" type="number" min="32" max="4096" value="1000"></label></div>${positionControls()}<div class="row2"><button class="btn" id="rotateLeft">${tr('左旋 90°','Rotate left')}</button><button class="btn" id="rotateRight">${tr('右旋 90°','Rotate right')}</button><button class="btn" id="flipHorizontal">${tr('水平翻转','Flip horizontal')}</button><button class="btn" id="flipVertical">${tr('垂直翻转','Flip vertical')}</button></div></div><div class="control-section"><h3>3 · ${tr('去背景与透明','Background removal')}</h3><div class="method-choice"><label><input type="radio" name="bgMethod" value="online" checked><span><strong>${tr('在线处理 · 无需下载','Online · no download')}</strong><small>${tr('图片会发送到服务器；单任务处理，完成后不保存图片。','Your image is sent to our server; one task at a time and no image is stored.')}</small></span></label><label><input type="radio" name="bgMethod" value="local"><span><strong>${tr('本地处理 · 更私密','Local · more private')}</strong><small>${tr('图片不上传；首次使用会下载约 40 MB 模型，之后浏览器缓存。','The image stays local; first use downloads a ~40 MB model, then caches it.')}</small></span></label></div><div class="row2"><button class="btn secondary" id="removeBg">${tr('开始去背景','Remove background')}</button><button class="btn" id="cornerClear">${tr('边角颜色转透明','Clear corner color')}</button></div><div class="progress hidden" id="progress"><i></i></div><label class="field"><span class="field-label"><span>${tr('背景','Background')}</span></span><select id="bgMode"><option value="transparent">${tr('透明','Transparent')}</option><option value="#ffffff">${tr('白色','White')}</option><option value="#000000">${tr('黑色','Black')}</option></select></label></div><div class="control-section"><h3>4 · ${tr('水印','Watermark')}</h3><label class="field"><span class="field-label">${tr('自定义文字','Custom text')}</span><input id="watermarkText" maxlength="40" placeholder="${tr('例如：小产品实验室','e.g. Tiny Product Lab')}"></label><div class="row2"><label class="field"><span class="field-label">${tr('文字颜色','Text color')}</span><input id="watermarkColor" type="color" value="#ffffff"></label><label class="field"><span class="field-label">${tr('文字大小','Text size')}</span><input id="watermarkSize" type="number" min="12" max="180" value="42"></label></div><label class="field"><span class="field-label">${tr('透明度','Opacity')}</span><input id="watermarkOpacity" type="range" min="10" max="100" value="85"></label><div class="row2"><label class="field"><span class="field-label">${tr('左右位置','Horizontal')}</span><input id="watermarkX" type="range" min="0" max="100" value="42"></label><label class="field"><span class="field-label">${tr('上下位置','Vertical')}</span><input id="watermarkY" type="range" min="0" max="100" value="88"></label></div><label class="upload compact"><strong>${tr('上传 Logo 水印','Upload logo watermark')}</strong><small>PNG / JPG / WebP</small><input id="watermarkLogo" type="file" accept="image/png,image/jpeg,image/webp"></label><button class="btn secondary wide" id="openCollage">${tr('制作拼图','Make a collage')}</button><button class="btn secondary wide" id="openBatch">${tr('批量压缩 / 转换','Batch compress / convert')}</button></div>`;
}
function positionControls(){return `<label class="field"><span class="field-label"><span>${tr('缩放','Scale')}</span><output id="zoomOut">100%</output></span><input id="zoom" type="range" min="50" max="250" value="100"></label><label class="field"><span class="field-label"><span>${tr('左右位置','Horizontal')}</span><output id="xOut">0</output></span><input id="offsetX" type="range" min="-100" max="100" value="0"></label><label class="field"><span class="field-label"><span>${tr('上下位置','Vertical')}</span><output id="yOut">0</output></span><input id="offsetY" type="range" min="-100" max="100" value="0"></label>`;}

function bindCommon(){ const b=document.querySelector('#lang'); if(b)b.onclick=()=>{lang=lang==='zh'?'en':'zh';localStorage.setItem('photo_lang',lang);mode==='home'?home():editor();};const feedback=document.querySelector('#feedback');if(feedback)feedback.onclick=()=>location.href='mailto:userfeedback@zohomail.com?subject='+encodeURIComponent(tr('图片工坊反馈','Photo Lab feedback'));const coffee=document.querySelector('#coffee'),dialog=document.querySelector('#coffeeDialog');if(coffee&&dialog)coffee.onclick=()=>dialog.showModal();const close=document.querySelector('#closeCoffee');if(close)close.onclick=()=>dialog.close(); }
function bindEditor(){
  installPreviewRulers();
  ['zoom','offsetX','offsetY','rotate','quality'].forEach(id=>{const el=document.querySelector('#'+id);if(el)el.oninput=()=>{const key=id==='quality'?'quality':id;state[key]=id==='quality'?+el.value/100:+el.value/(id==='zoom'?100:1);const out=document.querySelector('#'+id+'Out');if(out)out.textContent=id==='zoom'?el.value+'%':id==='quality'?el.value+'%':id==='rotate'?el.value+'°':el.value;draw();};});
  ['outW','outH'].forEach(id=>{const el=document.querySelector('#'+id);el.onchange=()=>{state[id==='outW'?'outputW':'outputH']=Math.max(32,Math.min(4096,+el.value||100));draw();};});
  const spec=document.querySelector('#spec'); if(spec)spec.onchange=()=>{if(spec.value!=='custom'){const s=specs[spec.value];state.outputW=s.w;state.outputH=s.h;document.querySelector('#outW').value=s.w;document.querySelector('#outH').value=s.h;draw();}};
  document.querySelectorAll('[data-bg]').forEach(b=>b.onclick=()=>{state.background=b.dataset.bg;document.querySelectorAll('[data-bg]').forEach(x=>x.classList.toggle('active',x===b));draw();});
  const custom=document.querySelector('#bgCustom');if(custom)custom.oninput=()=>{state.background=custom.value;draw();};
  const bgMode=document.querySelector('#bgMode');if(bgMode)bgMode.onchange=()=>{state.background=bgMode.value;draw();};
  document.querySelectorAll('input[name="bgMethod"]').forEach(input=>input.onchange=()=>{state.bgMethod=input.value;});
  const cropRatio=document.querySelector('#cropRatio');if(cropRatio)cropRatio.onchange=()=>{if(cropRatio.value!=='free'){const ratio=+cropRatio.value;state.outputH=Math.round(state.outputW/ratio);document.querySelector('#outH').value=state.outputH;draw();}};
  const rotateLeft=document.querySelector('#rotateLeft');if(rotateLeft)rotateLeft.onclick=()=>{state.rotate=(state.rotate-90+360)%360;draw();};
  const rotateRight=document.querySelector('#rotateRight');if(rotateRight)rotateRight.onclick=()=>{state.rotate=(state.rotate+90)%360;draw();};
  const flipHorizontal=document.querySelector('#flipHorizontal');if(flipHorizontal)flipHorizontal.onclick=()=>{state.flipX*=-1;draw();};
  const flipVertical=document.querySelector('#flipVertical');if(flipVertical)flipVertical.onclick=()=>{state.flipY*=-1;draw();};
  const fmt=document.querySelector('#format');fmt.onchange=()=>{state.format=fmt.value;document.querySelector('#qualityField').classList.toggle('hidden',fmt.value==='image/png');};
  document.querySelector('#removeBg').onclick=removeBg;
  const clear=document.querySelector('#cornerClear');if(clear)clear.onclick=clearCornerColor;
  ['watermarkText','watermarkColor','watermarkSize','watermarkOpacity','watermarkX','watermarkY'].forEach(id=>{const el=document.querySelector('#'+id);if(el)el.oninput=()=>{state[id]=['watermarkSize','watermarkX','watermarkY'].includes(id)?+el.value:id==='watermarkOpacity'?+el.value/100:el.value;draw();};});
  const watermarkText=document.querySelector('#watermarkText');if(watermarkText&&!document.querySelector('#watermarkTile')){const tile=document.createElement('label');tile.className='watermark-tile';tile.innerHTML=`<input id="watermarkTile" type="checkbox"><span><strong>${tr('重复平铺','Repeat as a pattern')}</strong><small>${tr('以倾斜文字覆盖整张图片','Repeat diagonal text across the image')}</small></span>`;watermarkText.closest('.field')?.after(tile);tile.querySelector('#watermarkTile').onchange=e=>{state.watermarkTile=e.target.checked;draw();};}
  const logo=document.querySelector('#watermarkLogo');if(logo){const logoFields=document.createElement('div');logoFields.className='logo-fields';logoFields.innerHTML=`<label class="field"><span class="field-label"><span>${tr('Logo 大小','Logo size')}</span><output id="logoSizeOut">22%</output></span><input id="logoSize" type="range" min="5" max="60" value="22"></label><label class="field"><span class="field-label"><span>${tr('Logo 透明度','Logo opacity')}</span><output id="logoOpacityOut">90%</output></span><input id="logoOpacity" type="range" min="10" max="100" value="90"></label><div class="row2"><label class="field"><span class="field-label">${tr('左右位置','Horizontal')}</span><input id="logoX" type="range" min="0" max="100" value="82"></label><label class="field"><span class="field-label">${tr('上下位置','Vertical')}</span><input id="logoY" type="range" min="0" max="100" value="82"></label></div>`;logo.closest('.upload').after(logoFields);['logoSize','logoOpacity','logoX','logoY'].forEach(id=>{const el=logoFields.querySelector('#'+id);el.oninput=()=>{state[id]=id==='logoOpacity'?+el.value/100:+el.value;const out=logoFields.querySelector('#'+id+'Out');if(out)out.textContent=el.value+'%';draw();};});logo.onchange=async e=>{const file=e.target.files[0];if(!file)return;try{state.watermarkLogo=await blobToImage(file);setStatus(tr('Logo 水印已载入。','Logo watermark loaded.'));draw();}catch{alert(tr('无法读取 Logo。','Could not read the logo.'));}};}
  const collage=document.querySelector('#openCollage');if(collage)collage.onclick=()=>openTool('collage');
  const batch=document.querySelector('#openBatch');if(batch)batch.onclick=()=>openTool('batch');
  document.querySelector('#download').onclick=()=>downloadCanvas('photo');
  const sheet=document.querySelector('#sheet');if(sheet)sheet.onclick=toggleSheetPreview;
  const downloadSheetButton=document.querySelector('#downloadSheet');if(downloadSheetButton)downloadSheetButton.onclick=downloadSheet;
  const canvas=document.querySelector('#canvas');let dragStart=null;if(canvas){canvas.onpointerdown=e=>{if(!state.source)return;dragStart={x:e.clientX,y:e.clientY,offsetX:state.offsetX,offsetY:state.offsetY};canvas.setPointerCapture?.(e.pointerId);};canvas.onpointermove=e=>{if(!dragStart)return;const rect=canvas.getBoundingClientRect();state.offsetX=Math.max(-100,Math.min(100,dragStart.offsetX+(e.clientX-dragStart.x)/rect.width*200));state.offsetY=Math.max(-100,Math.min(100,dragStart.offsetY+(e.clientY-dragStart.y)/rect.height*200));const x=document.querySelector('#offsetX'),y=document.querySelector('#offsetY'),xo=document.querySelector('#xOut'),yo=document.querySelector('#yOut');if(x)x.value=Math.round(state.offsetX);if(y)y.value=Math.round(state.offsetY);if(xo)xo.textContent=Math.round(state.offsetX);if(yo)yo.textContent=Math.round(state.offsetY);draw();};canvas.onpointerup=canvas.onpointercancel=()=>{dragStart=null;};}
}

function installPreviewRulers(){
  const wrap=document.querySelector('.canvas-wrap'),canvas=document.querySelector('#canvas');
  if(!wrap||!canvas||document.querySelector('#previewStage'))return;
  const stage=document.createElement('div');stage.className='preview-stage hidden';stage.id='previewStage';
  stage.innerHTML='<div class="ruler ruler-top" id="rulerTop"></div><div class="ruler ruler-left" id="rulerLeft"></div>';
  canvas.before(stage);stage.append(canvas);
  const physical=document.createElement('span');physical.className='status';physical.id='physicalSize';document.querySelector('.canvas-toolbar')?.append(physical);
  const sheet=document.querySelector('#sheet');if(sheet&&!document.querySelector('#downloadSheet')){const button=document.createElement('button');button.className='btn primary wide hidden';button.id='downloadSheet';button.textContent=tr('下载六寸排版照','Download 6-inch sheet');button.onclick=downloadSheet;sheet.closest('.row2')?.after(button);}
}

function updatePreviewMeasurements(width,height,widthInches,heightInches){
  const physical=document.querySelector('#physicalSize'),top=document.querySelector('#rulerTop'),left=document.querySelector('#rulerLeft');
  if(physical)physical.textContent=tr(`按 300 DPI · ${widthInches.toFixed(1)} × ${heightInches.toFixed(1)} 英寸`,`At 300 DPI · ${widthInches.toFixed(1)} × ${heightInches.toFixed(1)} in`);
  const marks=inches=>Array.from({length:Math.max(2,Math.ceil(inches)+1)},(_,i)=>`<span>${i}″</span>`).join('');
  if(top)top.innerHTML=marks(widthInches);if(left)left.innerHTML=marks(heightInches);
}

async function loadFile(file){
  if(!file)return; const allowed=['image/png','image/jpeg','image/webp'];
  if(!allowed.includes(file.type))return alert(tr('只支持 PNG、JPG 和 WebP。','Only PNG, JPG and WebP are supported.'));
  if(file.size>MAX_BYTES)return alert(tr('图片超过 10 MB。','The image exceeds 10 MB.'));
  try{const img=await blobToImage(file);if(img.naturalWidth*img.naturalHeight>MAX_PIXELS)throw new Error(tr('图片超过 2400 万像素。','The image exceeds 24 megapixels.'));state.file=file;state.source=img;state.processed=null;state.sheetPreview=false;document.querySelector('#editing').classList.remove('hidden');document.querySelector('#empty').classList.add('hidden');document.querySelector('#previewStage')?.classList.remove('hidden');document.querySelector('#canvas').classList.remove('hidden');setStatus(tr('照片已载入，元数据不会写入导出文件。','Photo loaded. Metadata will not be written to the export.'));draw();focusEntryControl();}catch(e){alert(e.message||e);}
}
function blobToImage(blob){return new Promise((resolve,reject)=>{const url=URL.createObjectURL(blob);state.objectUrls.push(url);const img=new Image();img.onload=()=>resolve(img);img.onerror=()=>reject(new Error(tr('无法读取这张图片。','Could not read this image.')));img.src=url;});}
async function removeBg(){
  if(!state.file)return; if(state.bgMethod==='online')return removeOnlineBg(); const btn=document.querySelector('#removeBg'),bar=document.querySelector('#progress');btn.disabled=true;bar.classList.remove('hidden');setStatus(tr('正在准备本地人像模型…','Preparing the local portrait model…'));
  try{const blob=await removeBackground(state.file,{model:'isnet_quint8',device:'cpu',output:{format:'image/png',quality:1,type:'foreground'},progress:(key,current,total)=>{const p=total?Math.round(current/total*100):15;bar.firstElementChild.style.width=p+'%';setStatus(tr(`正在本地处理：${p}%`,`Processing locally: ${p}%`));}});state.processed=await blobToImage(blob);bar.firstElementChild.style.width='100%';setStatus(tr('背景已去除，可以选择底色并微调位置。','Background removed. Choose a color and fine-tune the position.'));draw();}catch(e){console.error(e);alert(tr('智能去背景没有完成，请检查网络后重试。首次使用需要下载模型。','Background removal did not finish. Check your connection and try again; the first run downloads a model.'));setStatus(tr('去背景失败，原图仍可继续编辑。','Removal failed; you can still edit the original.'));}finally{btn.disabled=false;setTimeout(()=>bar.classList.add('hidden'),500);}
}

async function removeOnlineBg(){
  const btn=document.querySelector('#removeBg'),bar=document.querySelector('#progress');btn.disabled=true;bar.classList.remove('hidden');bar.firstElementChild.style.width='18%';setStatus(tr('正在加密上传图片并等待服务器处理…','Uploading securely and waiting for server processing…'));
  try{const form=new FormData();form.append('file',state.file,state.file.name||'photo');const response=await fetch(ONLINE_BG_API,{method:'POST',body:form,cache:'no-store'});if(!response.ok){const detail=await response.text();throw new Error(detail||response.statusText);}bar.firstElementChild.style.width='88%';const blob=await response.blob();state.processed=await blobToImage(blob);bar.firstElementChild.style.width='100%';setStatus(tr('在线去背景完成。服务器不会保存这张图片。','Online background removal complete. The server does not retain this image.'));draw();}catch(error){console.error(error);alert(tr('在线去背景暂时不可用。你可以改选“本地处理”，或稍后重试。','Online background removal is temporarily unavailable. Choose local processing or try again later.'));setStatus(tr('在线处理未完成，原图仍可继续编辑。','Online processing did not finish; you can still edit the original.'));}finally{btn.disabled=false;setTimeout(()=>bar.classList.add('hidden'),500);}
}
function clearCornerColor(){
  if(!state.source)return;const img=state.source,c=document.createElement('canvas');c.width=img.naturalWidth;c.height=img.naturalHeight;const x=c.getContext('2d',{willReadFrequently:true});x.drawImage(img,0,0);const d=x.getImageData(0,0,c.width,c.height),pts=[[0,0],[c.width-1,0],[0,c.height-1],[c.width-1,c.height-1]],avg=[0,0,0];pts.forEach(([px,py])=>{const i=(py*c.width+px)*4;avg[0]+=d.data[i];avg[1]+=d.data[i+1];avg[2]+=d.data[i+2];});avg.forEach((_,i)=>avg[i]/=4);for(let i=0;i<d.data.length;i+=4){const dist=Math.hypot(d.data[i]-avg[0],d.data[i+1]-avg[1],d.data[i+2]-avg[2]);if(dist<42)d.data[i+3]=Math.round(255*dist/42);}x.putImageData(d,0,0);c.toBlob(async b=>{state.processed=await blobToImage(b);draw();setStatus(tr('已将接近四角的颜色转为透明。','Colors close to the corners are now transparent.'));},'image/png');
}
function renderPhoto(canvas){
  const ctx=canvas.getContext('2d');canvas.width=state.outputW;canvas.height=state.outputH;ctx.clearRect(0,0,canvas.width,canvas.height);if(state.background!=='transparent'){ctx.fillStyle=state.background;ctx.fillRect(0,0,canvas.width,canvas.height);}const img=state.processed||state.source,iw=img.naturalWidth||img.width,ih=img.naturalHeight||img.height,base=Math.max(canvas.width/iw,canvas.height/ih),scale=base*state.zoom,dw=iw*scale,dh=ih*scale,cx=canvas.width/2+state.offsetX/100*canvas.width/2,cy=canvas.height/2+state.offsetY/100*canvas.height/2;ctx.save();ctx.translate(cx,cy);ctx.rotate(state.rotate*Math.PI/180);ctx.scale(state.flipX,state.flipY);ctx.drawImage(img,-dw/2,-dh/2,dw,dh);ctx.restore();drawWatermark(ctx,canvas);
}
function draw(){
  if(!state.source)return;const c=document.querySelector('#canvas');if(state.sheetPreview){const photo=document.createElement('canvas');renderPhoto(photo);const sheet=buildSixInchSheet(photo);c.width=sheet.width;c.height=sheet.height;c.getContext('2d').drawImage(sheet,0,0);updatePreviewMeasurements(c.width,c.height,6,4);document.querySelector('#dimensions').textContent=`${c.width} × ${c.height} px`;setStatus(tr('正在预览六寸排版照；确认后可下载。','Previewing the 6-inch print sheet; download when ready.'));}else{renderPhoto(c);document.querySelector('#dimensions').textContent=`${c.width} × ${c.height} px`;updatePreviewMeasurements(c.width,c.height,c.width/300,c.height/300);}updateChecks();
}
function drawWatermark(ctx,c){if(state.watermarkText){ctx.save();ctx.globalAlpha=state.watermarkOpacity;ctx.fillStyle=state.watermarkColor;ctx.font=`600 ${state.watermarkSize}px system-ui,sans-serif`;ctx.textAlign='center';ctx.textBaseline='middle';if(state.watermarkTile){const gap=Math.max(150,state.watermarkSize*7),span=Math.max(c.width,c.height)*1.5;ctx.translate(c.width/2,c.height/2);ctx.rotate(-Math.PI/6);for(let y=-span;y<=span;y+=gap)for(let x=-span;x<=span;x+=gap)ctx.fillText(state.watermarkText,x,y);}else ctx.fillText(state.watermarkText,c.width*state.watermarkX/100,c.height*state.watermarkY/100);ctx.restore();}if(state.watermarkLogo){const img=state.watermarkLogo,ratio=(img.naturalWidth||img.width)/(img.naturalHeight||img.height),w=c.width*state.logoSize/100,h=w/ratio;ctx.save();ctx.globalAlpha=state.logoOpacity;ctx.drawImage(img,c.width*state.logoX/100-w/2,c.height*state.logoY/100-h/2,w,h);ctx.restore();}}
function updateChecks(){const box=document.querySelector('#checks');if(!box||!state.source)return;const checks=[[state.source.naturalWidth>=600&&state.source.naturalHeight>=600,tr('原图分辨率充足','Source resolution is sufficient')],[!!state.processed,tr('背景已处理','Background has been processed')],[state.outputW>=200&&state.outputH>=200,tr('输出尺寸有效','Output size is valid')]];box.innerHTML=checks.map(([ok,text])=>`<div class="quality ${ok?'ok':''}">${ok?'✓':'○'} ${text}</div>`).join('');}
function setStatus(text){const el=document.querySelector('#status');if(el)el.textContent=text;}
function canvasBlob(canvas,format=state.format,quality=state.quality){return new Promise(resolve=>canvas.toBlob(resolve,format,quality));}
async function downloadCanvas(prefix){const c=document.querySelector('#canvas'),blob=await canvasBlob(c),ext=state.format==='image/png'?'png':state.format==='image/webp'?'webp':'jpg';saveBlob(blob,`${prefix}-${c.width}x${c.height}.${ext}`);setStatus(tr(`已导出 ${ext.toUpperCase()} · ${formatBytes(blob.size)}`,`Exported ${ext.toUpperCase()} · ${formatBytes(blob.size)}`));}
function buildSixInchSheet(photo){const sheet=document.createElement('canvas');sheet.width=1800;sheet.height=1200;const x=sheet.getContext('2d');x.fillStyle='#fff';x.fillRect(0,0,sheet.width,sheet.height);const gap=30,scale=Math.min(1,(sheet.height-gap*2)/photo.height),w=Math.round(photo.width*scale),h=Math.round(photo.height*scale),cols=Math.max(1,Math.floor((sheet.width-gap)/(w+gap))),rows=Math.max(1,Math.floor((sheet.height-gap)/(h+gap))),ox=Math.round((sheet.width-(cols*w+(cols-1)*gap))/2),oy=Math.round((sheet.height-(rows*h+(rows-1)*gap))/2);for(let r=0;r<rows;r++)for(let c=0;c<cols;c++){const px=ox+c*(w+gap),py=oy+r*(h+gap);x.drawImage(photo,px,py,w,h);x.strokeStyle='#bbb';x.setLineDash([6,5]);x.strokeRect(px-.5,py-.5,w+1,h+1);}return sheet;}
function toggleSheetPreview(){state.sheetPreview=!state.sheetPreview;const sheet=document.querySelector('#sheet'),download=document.querySelector('#download'),downloadSheetButton=document.querySelector('#downloadSheet');sheet.textContent=state.sheetPreview?tr('返回电子照','Back to image'):tr('预览六寸排版','Preview 6-inch sheet');download.classList.toggle('hidden',state.sheetPreview);downloadSheetButton?.classList.toggle('hidden',!state.sheetPreview);draw();}
async function downloadSheet(){const photo=document.createElement('canvas');renderPhoto(photo);const sheet=buildSixInchSheet(photo);saveBlob(await canvasBlob(sheet,'image/jpeg',.95),'id-photo-6inch-300dpi.jpg');setStatus(tr('已下载六寸排版照。','6-inch print sheet downloaded.'));}
function formatBytes(bytes){return bytes<1024?`${bytes} B`:bytes<1024*1024?`${(bytes/1024).toFixed(1)} KB`:`${(bytes/1024/1024).toFixed(2)} MB`;}
function saveBlob(blob,name){const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);}

home();
